# Supabase - publishable key is safe in the frontend (it's the new "anon" key)
VITE_SUPABASE_URL=https://agotjhsyoihwdouapcvw.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_Ick3rsllDe0uzhDtF0TU9g_SPWVZg2U

# NEVER put these in a VITE_ var - the VITE_ prefix exposes them to the browser.
# Supabase secret key  -> Supabase Edge Function env vars only
# Resend API key       -> Supabase Edge Function env vars only
# Sentry DSN           -> can be VITE_SENTRY_DSN (DSNs are public)
